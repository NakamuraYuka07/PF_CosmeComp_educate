<table class='table'>
  <tr><%= attachment_image_tag user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.nickname %></th>
  </tr>

</table>
  <% if user.id == current_user.id %>
  <%= link_to 'マイページ',user_path(user),class: "btn btn-outline-secondary btn-block fas fa-user-cog user_#{user.id}" %>
  <% end %>